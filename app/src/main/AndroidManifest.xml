<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="whsct.test">

    <!--
    Permissions

    TODO: Request permissions on app launch.
    -->

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/droplet"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".settingMenu" />
        <activity android:name=".emergencyScreen" />
        <activity android:name=".volunteerLogin" />
        <activity android:name=".volunteerMenu" />
        <activity android:name=".volunteerSetting" />
        <activity android:name=".newNote" />
        <activity android:name=".userRating" />
        <activity android:name=".incomingCall" />
        <activity android:name=".newAccount"></activity>
    </application>

</manifest>